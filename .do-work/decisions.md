2026-06-24 | REQ-006 | ClaudeClient.message() takes documents as plain array + tool as untyped array; callers MUST pass DocumentBlock instances and compose the correct Anthropic tool shape (name/description/input_schema) themselves | PHP has no runtime generic enforcement; a malformed tool array yields a silent HTTP 400, so shape correctness is the caller's responsibility (REQ-008 extraction, REQ-014 explanation)
2026-06-24 | REQ-008 | ExtractedInvoice exposes camelCase value props (invoiceDate, dueDate) but the per-field confidence map is keyed snake_case (confidenceFor('invoice_date')); ServiceCategory is a backed enum; scalar confidence fields are supplier,abn,invoice_date,due_date,subtotal,gst,total | downstream consumers (validators REQ-009-012, scorer REQ-013, explanation REQ-014, result page REQ-017) must use the property for values and snake_case keys for confidence
2026-06-24 | UR-004/REQ-021 | payment-allocated invoices (Total = net balance after payment/credit line items) reconcile Total via the line-item sum, not subtotal+GST; suppress arithmetic check (c)'s total error when line items corroborate the Total and emit one info-severity finding instead | arithmetic-only, extends the REQ-019 inclusive-line-items decision to the total check; no new extraction field
2026-06-24 | UR-004/REQ-022 | info-severity validation findings are contextual, not blocking: ExplanationService frames info-only findings with all-clear wording and excludes them from supplier-fix advice; ValidationPanel renders them in a Notes section and excludes them from the issue count; ConfidenceScorer already ignores them | a deliberate info severity exists across DTO + frontend Severity type and must be handled consistently end-to-end
2026-06-25 | UR-006/REQ-026 | check (a) line-item reconciliation excludes adjustment lines (negative amount OR payment/credit/refund/fee description keyword) via a PHP-side classifier in ArithmeticValidator; charge-only reconciliation is ADDITIVE to the existing full-sum check, and an info-severity subtotal note is emitted when charges reconcile but the full sum does not — Option A chosen over extraction-tagged line kind to respect REQ-021's "no new extraction field" | gross-total invoices (Total = gross charges, payment/fee as memo rows) are the mirror of REQ-021's net-total case; additive widening cannot regress a passing invoice
